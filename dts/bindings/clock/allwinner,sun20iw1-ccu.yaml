$schema: http://devicetree.org/meta-schemas/core.yaml#
title: Allwinner sun20iw1 clock controller used by SyterKit

properties:
  compatible:
    const: allwinner,sun20iw1-ccu
  reg:
    type: reg
  reg-names:
    type: string-array
    const: [ccu]
    description: Ordered as ccu.
  status:
    type: string
    enum: [okay, disabled]

required:
  - compatible
  - reg
  - reg-names

dt2c,device: true
